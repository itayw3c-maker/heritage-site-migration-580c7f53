# מחיקת המאמר "שמאי רכוש מומלץ – איך בוחרים נכון?"

## מה מצאתי
המאמר אינו קובץ תוכן מהמיגרציה (`public/content/`) — הוא מאמר שפורסם דרך ה-API ונמצא בבסיס הנתונים:

- slug: `recommended-property-appraiser`
- כותרת: שמאי רכוש מומלץ – איך בוחרים נכון? 7 קריטריונים שחובה לבדוק
- סטטוס: published (פורסם ב-05.08.2026)

הוא מוצג בכתובת השורש `/recommended-property-appraiser/` דרך ה-catch-all, מופיע ב-`blog-sitemap.xml` ובארכיוני הקטגוריה שממזגים מאמרי DB.

## מה אעשה
1. מחיקה מלאה של השורה מטבלת `posts` בבסיס הנתונים (מחיקה, לא רק הסתרה).
2. אימות שהשורה נעלמה ושהכתובת `/recommended-property-appraiser/` מחזירה 404 (הדף המעוצב עם `noindex`), וכן שהמאמר לא מופיע יותר במפת האתר של הבלוג ובארכיונים.

אין שינויי קוד — רק הסרת הרשומה מהנתונים.

## הערה
אם תעדיף שהמאמר יישמר במערכת אך לא יוצג באתר (למשל להחזרה עתידית), אפשר במקום מחיקה לשנות את הסטטוס ל-`draft`. אמור לי ואעשה כך.
